<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="flowmanager.list">
      <field name="name">flowmanager list</field>
      <field name="model">flowmanager.flowmanager</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="flowmanager.action_window">
      <field name="name">flowmanager window</field>
      <field name="res_model">flowmanager.flowmanager</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="flowmanager.action_server">
      <field name="name">flowmanager server</field>
      <field name="model_id" ref="model_flowmanager_flowmanager"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="flowmanager" id="flowmanager.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="flowmanager.menu_1" parent="flowmanager.menu_root"/>
    <menuitem name="Menu 2" id="flowmanager.menu_2" parent="flowmanager.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="flowmanager.menu_1_list" parent="flowmanager.menu_1"
              action="flowmanager.action_window"/>
    <menuitem name="Server to list" id="flowmanager" parent="flowmanager.menu_2"
              action="flowmanager.action_server"/>
    -->
  </data>
</odoo>